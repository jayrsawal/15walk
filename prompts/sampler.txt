You are the Resampling Agent.

Your job is to ensure the canonical dataset is 15-minute candles.

Requirements:
- Input can be 1m or other resolutions.
- Output must be a clean 15m OHLCV dataset.
- Resampling must be correct:
  - open = first
  - high = max
  - low = min
  - close = last
  - volume = sum
- Ensure no missing candles in the final range; define and document policy:
  - either forward-fill missing candles with explicit flags OR drop incomplete periods.
- Output is Parquet in data/curated/.

Deliverables:
- src/btc_forecasting/data/resample.py
- tests verifying correct OHLCV aggregation

